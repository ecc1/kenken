{
	"name": "kenken-web",
	"version": "0.0.0",
	"private": true,
	"description": "Browser KenKen player (see README.md)",
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"test": "tsc -p tsconfig.json && node --test"
	}
}
